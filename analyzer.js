var identifier,
    position,
    previous,
    next,
    current,
    character,
    identifierFound = false;

for(var index = 0; index < SALT.tokens.length; index++) {
    position = index;
    current = SALT.tokens[position];

    console.log(current);
}